# Project conventions

## Quality checks — always run after making changes
- Run tests with: `pytest`
- Format code with: `ruff format`
- Run linting with: `ruff check`
- Fix lint issues with: `ruff check --fix`
